%% AION-1 talk — beamer/moloch
%% Build: latexmk -lualatex presentation.tex   (or use the top-level Makefile)

\documentclass[10pt]{beamer}

\usetheme{moloch}

% ── fonts (Fira Sans via fontspec; requires LuaLaTeX or XeLaTeX) ──────────────
\usepackage{fontspec}
\setsansfont{Fira Sans}[
  BoldFont = {Fira Sans Bold},
  ItalicFont = {Fira Sans Italic},
]
\setmonofont{Fira Mono}
\usefonttheme[onlymath]{serif}

% ── packages ──────────────────────────────────────────────────────────────────
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{amsmath,amssymb}
\usepackage{xcolor}
\usepackage{tikz}

% ── bibliography ──────────────────────────────────────────────────────────────
\usepackage[backend=biber, style=authoryear, maxnames=2]{biblatex}
\addbibresource{bibliography/presentation-bibliography.bib}
% To also draw on the AION-1 paper bibliography (available after make fetch):
% \addbibresource{../resources/aion-1-paper/mmoma.bib}

% ── graphicspath ──────────────────────────────────────────────────────────────
\graphicspath{%
  {../resources/papers/aion-1-paper/source/figures/}%
  {../resources/papers/melchior2023spender/source/figures/}%
  {../resources/papers/ginolfi2024moons/source/figures/}%
  {../resources/papers/duev2019realbogus/source/}%
  {../resources/papers/hayat2021selfsupervised/source/figures/}%
  {../resources/papers/walmsley2022gzdecals/source/figures/}%
  {../resources/papers/smith2024astropt/source/figures/}%
  {../resources/papers/astroclip2023/source/figures/}%
  {../resources/papers/4m2023/source/resources/}%
  {../resources/papers/mentzer2023fsq/source/figs/}%
  {../resources/papers/rombach2021ldm/source/img/}%
  {../resources/papers/oord2017vqvae/source/figures/}%
  {../resources/papers/maskgit/source/figures/}%
  {../resources/rendered/tables/aion-1-paper/}%
  {../resources/}%
  {../resources/images/}%
  {../resources/papers/}%
}

% ── moloch customisation ──────────────────────────────────────────────────────
\molochset{progressbar=frametitle}
\setbeamertemplate{frame numbering}[fraction]

% ── metadata ──────────────────────────────────────────────────────────────────
\title{AION-1: Omnimodal Foundation Model for Astronomical Sciences}
\author{Tom Hehir}
\institute{Institute of Astronomy, University of Cambridge \\
           Kavli Institute for Cosmology, Cambridge \\
           Clare College \\
           Polymathic AI}
\date{2 March 2026}

% ── convenience macros ────────────────────────────────────────────────────────
\newcommand{\aion}{AION-1}

% ══════════════════════════════════════════════════════════════════════════════
\begin{document}

\begin{frame}[plain, noframenumbering]
  \titlepage
  \begin{tikzpicture}[remember picture, overlay]
    \node[anchor=south, yshift=10mm] at (current page.south) {%
      \includegraphics[height=2.1em]{logo-cambridge.pdf}\hspace{1.2em}%
      \includegraphics[height=2.1em]{logo-ioa.png}\hspace{1.2em}%
      \includegraphics[height=2.1em]{logo-kicc.png}\hspace{1.2em}%
      \includegraphics[height=2.1em]{logo-clare.pdf}\hspace{1.2em}%
      \includegraphics[height=2.1em]{polymathic-logo.pdf}%
    };
    \node[anchor=south, yshift=3mm] at (current page.south) {%
      \small\href{https://github.com/tom-hehir/aion-talk}%
      {\texttt{github.com/tom-hehir/aion-talk}}%
    };
  \end{tikzpicture}
\end{frame}

% ── §2 Abstract ───────────────────────────────────────────────────────────────
\begin{frame}{Abstract}
  \centering
  \includegraphics[height=0.82\textheight, trim={0 207pt 0 0}, clip]{titlepage.pdf}
  \par\vspace{0.5em}
  \href{https://arxiv.org/abs/2510.17960}{%
    \includegraphics[height=1.6em]{badge-arxiv.pdf}%
  }%
  \hspace{0.5em}%
  \href{https://neurips.cc/virtual/2025/loc/san-diego/poster/119776}{%
    \includegraphics[height=1.6em]{badge-neurips.pdf}%
  }
\end{frame}

% ── §3 Team ───────────────────────────────────────────────────────────────────
\begin{frame}{Team}
  \centering
  \includegraphics[width=\textwidth]{team-collage.png}
  \par\vspace{0.5em}
  \href{https://polymathic-ai.org}{%
    \includegraphics[height=1.2em]{polymathic-logo.pdf}%
  }
\end{frame}

% ── §3 Status Quo ─────────────────────────────────────────────────────────────
\begin{frame}[standout]
  Motivation
\end{frame}

\begin{frame}{Status quo: survey-specific classification}
  \begin{columns}[T]
    \begin{column}{0.50\textwidth}
      \centering
      \includegraphics[width=\linewidth, height=0.72\textheight, keepaspectratio]{fig-braai3.png}
    \end{column}
    \begin{column}{0.47\textwidth}
      \textbf{Task- and data-specific models}\\[0.6em]
      VGG6 on ZTF science/reference/difference image triplets for
      real-bogus discrimination \parencite{duev2019realbogus}.\\[0.5em]
      CNNs for strong gravitational lens finding
      \parencite{metcalf2019lensfinding}.\\[0.5em]
      Each classifier is coupled to a specific survey and task; new
      surveys or science cases require retraining from scratch.
    \end{column}
  \end{columns}
\end{frame}

\begin{frame}{Status quo: data-specific regression}
  \begin{columns}[T]
    \begin{column}{0.50\textwidth}
      \centering
      \includegraphics[width=\linewidth, height=0.72\textheight, keepaspectratio]{nn-sketch.png}
    \end{column}
    \begin{column}{0.47\textwidth}
      \textbf{Task- and data-specific models}\\[0.6em]
      Multi-task net with probabilistic outputs for joint redshift and
      galaxy property estimation on simulated MOONS spectra
      \parencite{ginolfi2024moons}.\\[0.5em]
      Bayesian SED emulation \parencite{alsing2020speculator}: fast
      surrogate for full spectral energy distribution fitting, but
      trained for a fixed survey and parameter set.
    \end{column}
  \end{columns}
\end{frame}

\begin{frame}{Status quo: physical inductive bias}
  \begin{columns}[T]
    \begin{column}{0.50\textwidth}
      \centering
      \includegraphics[width=\linewidth, height=0.72\textheight, keepaspectratio]{sketch.pdf}
    \end{column}
    \begin{column}{0.47\textwidth}
      \textbf{Task- and data-specific models}\\[0.6em]
      Architecture encodes redshift and instrument response
      \textbf{explicitly} in the model design
      \parencite{melchior2023spender}, or via the training objective
      \parencite{korber2023pinion}.\\[0.5em]
      Strong physical priors improve performance on the target task but
      limit reuse across surveys or science cases.
    \end{column}
  \end{columns}
\end{frame}

% ── §4 Prior Foundation Model Efforts ─────────────────────────────────────────
\begin{frame}[standout]
  Prior Work
\end{frame}

\begin{frame}{Prior work: supervised pre-training}
  \begin{columns}[T]
    \begin{column}{0.45\textwidth}
      \centering
      \includegraphics[width=\linewidth]{dr5_tree_with_icons_new_format_larger_arrows_inverted.png}
    \end{column}
    \begin{column}{0.52\textwidth}
      \textbf{Large, rich supervised label sets}\\[0.6em]
      Galaxy Zoo DECaLS \parencite{walmsley2022gzdecals} collected
      volunteer morphology classifications across a branching question
      tree for 314,000 galaxies.\\[0.5em]
      Zoobot \parencite{walmsley2023zoobot} distils this into a
      reusable library: pretrain on GZ labels, then transfer to new
      morphology tasks with few labels.\\[0.5em]
      Supervised scaling laws on galaxy images show smooth power-law
      behaviour up to 840k examples \parencite{walmsley2024scalinglaws}.
    \end{column}
  \end{columns}
\end{frame}

\begin{frame}{Prior work: contrastive learning}
  \centering
  \includegraphics[width=\textwidth]{network_illustration.pdf}
  \par\smallskip
  {\small
    \textbf{Self-supervised representations:}
    contrastive SSL on SDSS photometry \parencite{hayat2021selfsupervised};
    extended to multi-modal with AstroCLIP \parencite{parker2024astroclip}
    and AstroM$^3$ \parencite{rizhko2024astromm}.
  }
\end{frame}

% ── Prior Work: AstroCLIP ─────────────────────────────────────────────────────
\begin{frame}{Prior work: multimodal contrastive learning}
  \begin{columns}[T]
    \begin{column}{0.55\textwidth}
      \centering
      \includegraphics[width=\linewidth, height=0.78\textheight, keepaspectratio]{im_embedding.pdf}
    \end{column}
    \begin{column}{0.42\textwidth}
      \textbf{Cross-modal contrastive alignment}\\[0.6em]
      AstroCLIP \parencite{parker2024astroclip} aligns galaxy images and
      spectra into a shared embedding space using a CLIP-style
      contrastive objective.\\[0.5em]
      Enables zero-shot cross-modal retrieval and downstream parameter
      estimation without task-specific labels.\\[0.5em]
      Extended to three modalities (image, spectrum, photometry)
      in AstroM$^3$ \parencite{rizhko2024astromm}.
    \end{column}
  \end{columns}
\end{frame}

\begin{frame}{Prior work: generative models}
  \begin{columns}[T]
    \begin{column}{0.55\textwidth}
      \centering
      \includegraphics[width=\linewidth]{scaling.pdf}
    \end{column}
    \begin{column}{0.42\textwidth}
      \textbf{Autoregressive pre-training at scale}\\[0.6em]
      AstroPT \parencite{smith2024astropt} tokenises galaxy images into
      spiral-ordered patches and trains a GPT-style transformer on 8.6M
      DESI Legacy Survey images; downstream performance follows a clear
      scaling law from 1M to 2.1B parameters.\\[0.5em]
      Also: Maven \parencite{zhang2024maven} for supernovae light
      curves; transformer foundation model for stellar spectra
      \parencite{leung2023stellar}.
    \end{column}
  \end{columns}
\end{frame}

% ── §5 Introducing AION-1 ─────────────────────────────────────────────────────
\begin{frame}[standout]
  AION-1
\end{frame}

\begin{frame}{Introducing AION-1}
  \centering
  \includegraphics[width=\textwidth, height=0.82\textheight, keepaspectratio]{aion.png}
\end{frame}

% ── §6 Data ───────────────────────────────────────────────────────────────────
\begin{frame}{Data}
  \begin{columns}[T]
    \begin{column}{0.44\textwidth}
      \centering
      \includegraphics[width=\linewidth]{aion_fig2_real_samples_no_logo.png}
    \end{column}
    \begin{column}{0.54\textwidth}
      {\small 39 modalities, ${\sim}200$M observations of stars, galaxies,
        and quasars from the \textbf{Multimodal Universe}
        \parencite{multimodaluniverse2024}.}\\[0.4em]
      \small
      \begin{tabularx}{\linewidth}{@{}lX@{}}
        \toprule
        \textbf{Survey} & \textbf{Modalities} \\ \midrule
        Legacy Survey &
          4-band images; $g,r,i,z$ fluxes; WISE $W1$–$W4$; $E(B{-}V)$;
          ellipticity $(e_1,e_2)$; $R_{\rm eff}$ \\[3pt]
        HSC &
          5-band images; $g,r,i,z,y$ fluxes \& extinction; shape catalog \\[3pt]
        SDSS & Optical spectrum; redshift $z$ \\[3pt]
        DESI & Optical spectrum; redshift $z$ \\[3pt]
        Gaia &
          BP/RP spectra; parallax $\varpi$; sky coords;
          $G$, BP, RP fluxes \\ \bottomrule
      \end{tabularx}
    \end{column}
  \end{columns}
\end{frame}

% ── §7 Architecture & Objective ───────────────────────────────────────────────
\begin{frame}{Architecture: 4M}
  \begin{columns}[T]
    \begin{column}{0.52\textwidth}
      \centering
      \includegraphics[width=\linewidth, height=0.42\textheight, keepaspectratio]{aion.png}
      \par\smallskip
      \includegraphics[width=\linewidth, height=0.34\textheight, keepaspectratio]{method_figure.pdf}
      \par\smallskip
      {\tiny 4M architecture \parencite{mizrahi2023}}
    \end{column}
    \begin{column}{0.45\textwidth}
      \textbf{Encoder token embedding}
      \begin{equation*}
        \mathbf{e}_t^{(\mathrm{enc})} = \mathrm{Embed}_i(x_t^i) + \mathbf{m}_i + \mathbf{p}_t
      \end{equation*}
      \textbf{Decoder token embedding}
      \begin{equation*}
        \mathbf{e}_t^{(\mathrm{dec})} = \mathbf{m}_i + \mathbf{p}_t
      \end{equation*}
      {\small
        $\mathbf{m}_i$: learnable modality embedding, one per modality
        \emph{and} survey (encodes instrument provenance).\\[0.4em]
        $\mathbf{p}_t$: positional embedding.\\[0.4em]
        Decoder omits $\mathrm{Embed}_i(x_t^i)$ — it receives only
        modality $+$ position, not the token value.
      }
    \end{column}
  \end{columns}
\end{frame}

\begin{frame}{Tokenization: autoencoders}
  \centering
  \includegraphics[width=\textwidth, height=0.78\textheight, keepaspectratio]{autoencoder_schema.jpg}
  \par\smallskip
  {\small An autoencoder compresses input $\mathbf{x}$ to a low-dimensional
    latent code $\mathbf{z}$ via an encoder, then reconstructs $\hat{\mathbf{x}}$
    via a decoder. Tokenisation uses the discrete latent codes as input tokens
    for the transformer.}
\end{frame}

\begin{frame}{Tokenization: discrete codes}
  \begin{columns}[T]
    \begin{column}{0.54\textwidth}
      \centering
      \includegraphics[width=\linewidth]{overview_v2_a.pdf}
      \par\smallskip
      {\small \textbf{FSQ \parencite{mentzer2023fsq}}: project latent into
        an integer grid $L_1\!\times\!\cdots\!\times\!L_d$; no codebook
        collapse.}
    \end{column}
    \begin{column}{0.43\textwidth}
      \centering
      \includegraphics[width=\linewidth]{scalar-quantization-binning.png}
      \par\smallskip
      {\small \textbf{Scalar binning}: map via empirical CDF to
        $\mathcal{N}(0,1)$, then equal-width bins $\Rightarrow$ equal
        probability mass per bin.}
    \end{column}
  \end{columns}
  \par\smallskip
  {\small
    \textbf{Images}: FSQ (1024 codes)\enspace
    \textbf{Spectra}: LFQ (1024 codes)\enspace
    \textbf{Scalars}: CDF binning (1024 bins)
  }
\end{frame}

\begin{frame}{Tokenization: tokenizer objective}
  \textbf{Image \& spectrum flux loss} — inverse-variance-weighted Gaussian NLL,
  leveraging known per-pixel noise from survey pipelines:
  \begin{equation*}
    \mathcal{L}_\mathrm{NLL}
    = \sum_i \frac{1}{2}
      \left\|
        \boldsymbol{\Sigma}_i^{-\frac{1}{2}}\,\mathbf{m}_i
        \!\left(\mathbf{x}_i - \underbrace{\mathrm{Dec}_\theta\!\left(\mathrm{Enc}_\phi(\mathbf{x}_i)\right)}_{\hat{\mathbf{x}}_i}\right)
      \right\|_2^2
  \end{equation*}
  \vspace{0.4em}
  \textbf{Spectrum tokenizer adds a mask loss:}
  \begin{itemize}
    \item Binary cross-entropy between predicted reliability map
          $\hat{\mathbf{m}}(\lambda)$ and ground-truth sky/quality mask
          $\mathbf{m}(\lambda)$
    \item Likelihood-based, but no principled way to weight against the
          flux NLL
  \end{itemize}
\end{frame}

\begin{frame}{Image tokenizer}
  \centering
  \includegraphics[width=\textwidth, height=0.78\textheight, keepaspectratio]{legacy_survey_comparison.png}
  \par\smallskip
  {\small FSQ autoencoder on 9-channel multi-survey images.
    Columns: original, reconstruction, Tractor model, $r$-band residuals.}
\end{frame}

\begin{frame}{Spectrum tokenizer}
  \centering
  \includegraphics[width=\textwidth, height=0.78\textheight, keepaspectratio]{spectrum_autoencoding.png}
  \par\smallskip
  {\small LFQ autoencoder on DESI spectra. Top: flux (black = input,
    red = reconstruction). Bottom: residuals in units of $\sigma$.}
\end{frame}

\begin{frame}{Scalar tokenizer}
  \begin{columns}[T]
    \begin{column}{0.48\textwidth}
      \centering
      \includegraphics[width=\linewidth]{scalar-quantization-binning.png}
    \end{column}
    \begin{column}{0.49\textwidth}
      Map each scalar to a standard normal variate via the empirical CDF:
      \begin{equation*}
        z_i \;=\; \Phi^{-1}\!\bigl(F_x(x_i)\bigr)
      \end{equation*}
      Fixed centroids at equally spaced normal quantiles:
      \begin{equation*}
        c_k \;=\; \Phi^{-1}\!\!\left(\frac{k - \tfrac{1}{2}}{K}\right), \quad K = 1024
      \end{equation*}
      Reconstruction inverts the mapping:
      \begin{equation*}
        \hat{x}_i \;=\; F_x^{-1}\!\bigl(\Phi(c_i)\bigr)
      \end{equation*}
      {\small No parameters learned; no loss required. Equal probability
        mass per bin adapts automatically to long tails.}
    \end{column}
  \end{columns}
\end{frame}

\begin{frame}{Pre-training objective: masked token modelling}
  \begin{center}
    \begin{align*}
      \mathcal{L}(\theta)
      &= -H(f, p_\theta) \\
      &= -\mathbb{E}_{(\mathbf{x}^{\mathrm{targ}},\,\mathbf{x}^{\mathrm{cond}})\sim f}
          \!\left[\log p_\theta(\mathbf{x}^{\mathrm{targ}} \mid \mathbf{x}^{\mathrm{cond}})\right] \\
      &\simeq -\sum_{i=1}^{N} \log p_\theta\!\left(\mathbf{x}_i^{\mathrm{targ}} \mid \mathbf{x}_i^{\mathrm{cond}}\right)
    \end{align*}
  \end{center}
  \vspace{0.3em}
  \begin{columns}[T]
    \begin{column}{0.48\textwidth}
      \textbf{Input} (budget: 256 tokens)
      \begin{enumerate}\small
        \item Select primary modality at random
        \item Uniformly sample a \# of tokens $n$
        \item Randomly sample $n$ tokens from that modality
        \item Fill remainder by randomly sampling tokens from all other modalities
      \end{enumerate}
    \end{column}
    \begin{column}{0.48\textwidth}
      \textbf{Output} (budget: 128 tokens)
      \begin{enumerate}\small
        \item Select primary modality at random (may differ from input)
        \item Sample \# of tokens $n$ via Beta distribution (skewed low)
        \item Randomly sample $n$ tokens from that modality
        \item Fill remainder by randomly sampling tokens from all other modalities
      \end{enumerate}
    \end{column}
  \end{columns}
  \vspace{0.4em}
  Discretisation is necessary: cross-entropy over discrete tokens gives a
  \textbf{proper probabilistic objective} — continuous latents do not.
\end{frame}

% ── §8 Model Training & Variants ──────────────────────────────────────────────
\begin{frame}{Model family \& training}
  \begin{columns}[T]
    \begin{column}{0.50\textwidth}
      \centering
      \includegraphics[width=\linewidth]{eval_loss.png}
      \par\smallskip
      {\small Eval loss vs.\ training steps for three model sizes, with and
        without the Gaia stellar set.}
    \end{column}
    \begin{column}{0.48\textwidth}
      \centering
      \includegraphics[width=\linewidth]{tab_model-size.pdf}
    \end{column}
  \end{columns}
  \vspace{0.4em}
  {\small\textbf{Training:} AdamW ($\beta_1{=}0.9$, $\beta_2{=}0.95$, wd\,$0.05$),
    warmup + cosine decay, peak lr $2\!\times\!10^{-4}$, 205k steps, batch 8192,
    bfloat16, FSDP ZeRO-2, 64–288 H100 GPUs (1.5–3.5 days).}
\end{frame}

% ── §8.5 Embedding Extraction ─────────────────────────────────────────────────
\begin{frame}{Embedding extraction: pooling}
  \textbf{Protocol}: freeze the encoder; pool the contextualized output sequence
  $\mathbf{Z}=\{\mathbf{z}_1,\dots,\mathbf{z}_T\},\;\mathbf{z}_t\in\mathbb{R}^d$
  to a single object-level vector $\mathbf{e}\in\mathbb{R}^d$.\\[0.8em]
  \begin{columns}[T]
    \begin{column}{0.48\textwidth}
      \textbf{Mean pooling}
      \begin{equation*}
        \mathbf{e} \;=\; \frac{1}{T}\sum_{t=1}^{T}\mathbf{z}_t
      \end{equation*}
      {\small Parameter-free. Used for morphology classification and
        similarity retrieval.}
    \end{column}
    \begin{column}{0.48\textwidth}
      \textbf{Attentive pooling}
      \begin{equation*}
        \mathbf{A} = \mathrm{softmax}\!\Bigl(\tfrac{\mathbf{Q}\mathbf{K}^\top}{\sqrt{d}}\Bigr),\quad
        \mathbf{e} = \mathbf{A}\mathbf{V}
      \end{equation*}
      {\small $\mathbf{Q},\mathbf{K},\mathbf{V}$: learnable projections.
        Used for galaxy and stellar property estimation.}
    \end{column}
  \end{columns}
  \vspace{0.6em}
  {\small\textbf{Multi-modal embeddings:} concatenate tokens from any subset
    of modalities before the frozen encoder — cross-modal context was learned
    during pre-training. No extra fusion module required.}
\end{frame}

% ── §9 Downstream Tasks ───────────────────────────────────────────────────────
\begin{frame}[standout]
  Results
\end{frame}

% ── 9.1 Out-of-the-Box: Redshift Estimation ───────────────────────────────────
\begin{frame}{Galaxy redshift estimation (generative)}
  {\small \textbf{Generation}: iterative masked decoding under a cosine
    schedule (MaskGIT-style).  The redshift token is predicted from
    increasingly rich conditioning contexts; posteriors contract accordingly.}
  \vspace{0.3em}
  \begin{columns}[T]
    \begin{column}{0.32\textwidth}
      \centering
      \includegraphics[width=\linewidth]{redshift_photometry.png}
      \par\smallskip
      {\footnotesize Photometry only\\(broad $p(z)$)}
    \end{column}
    \begin{column}{0.32\textwidth}
      \centering
      \includegraphics[width=\linewidth]{redshift_image.png}
      \par\smallskip
      {\footnotesize $+$ Multiband image\\(tighter interval)}
    \end{column}
    \begin{column}{0.32\textwidth}
      \centering
      \includegraphics[width=\linewidth]{redshift_spectrum.png}
      \par\smallskip
      {\footnotesize $+$ DESI spectrum\\(near-point estimate)}
    \end{column}
  \end{columns}
\end{frame}

% ── 9.1 Out-of-the-Box: Spectral Super-Resolution ────────────────────────────
\begin{frame}{Spectral super-resolution}
  \centering
  \includegraphics[width=\textwidth, height=0.78\textheight, keepaspectratio]{spectrum_superresolution.pdf}
  \par\smallskip
  {\small Gaia BP/RP (low-res, red) $\to$ DESI spectrum posterior (blue)
    vs.\ ground-truth (black). Key spectral lines are reproduced in location,
    width, and amplitude.}
\end{frame}

% ── 9.2 Galaxy Parameter Estimation ──────────────────────────────────────────
\begin{frame}{Galaxy parameter estimation}
  {\small \textbf{Protocol}: freeze encoder; pool tokens; train lightweight MLP head.
    $R^2$ on PROVABGS \parencite{hahn2023provabgs} (${\sim}120\text{k}$ galaxies).}
  \vspace{0.3em}
  \centering
  \includegraphics[width=0.98\textwidth]{tab_galaxy_properties_full.pdf}
\end{frame}

% ── 9.2 Galaxy Morphology Classification ─────────────────────────────────────
\begin{frame}{Galaxy morphology classification}
  {\small GZ10 dataset: 10 classes, ${\sim}8\,000$ galaxies.
    Frozen AION-1 encoder $+$ lightweight MLP head.}
  \vspace{0.3em}
  \begin{columns}[T]
    \begin{column}{0.50\textwidth}
      \centering
      \includegraphics[width=0.92\linewidth]{tab_wrap_acc.pdf}
    \end{column}
    \begin{column}{0.48\textwidth}
      {\small
        \begin{itemize}
          \item AION-1-L matches ZooBot within 2.4 pp despite seeing
                two orders of magnitude fewer labeled galaxy images
          \item ZooBot was pre-trained on ${\sim}300\text{k}$ galaxy images
                with GZ-DECaLS volunteer labels \parencite{walmsley2022gzdecals};
                acts as an approximate upper bound
          \item DINOv2 trained on natural images transfers poorly to
                galaxy morphology ($-15.8$ pp vs.\ AION-1-L)
        \end{itemize}
      }
    \end{column}
  \end{columns}
\end{frame}

% ── 9.2 Galaxy Image Segmentation ────────────────────────────────────────────
\begin{frame}{Galaxy image segmentation}
  \begin{columns}[T]
    \begin{column}{0.54\textwidth}
      \centering
      \includegraphics[width=\linewidth]{aion_gz3d_vis_top.pdf}\\[1mm]
      \includegraphics[width=\linewidth]{aion_gz3d_vis_bot.pdf}
    \end{column}
    \begin{column}{0.43\textwidth}
      Galaxy Zoo 3D citizen-science spiral arm and bar annotations.
      Lightweight convolutional upsampler on frozen AION-1 embeddings.
      \vspace{0.6em}

      \begin{equation*}
        \mathrm{IoU}
        \;=\;
        \frac{|M_\text{pred}\cap M_\text{true}|}
             {|M_\text{pred}\cup M_\text{true}|}
      \end{equation*}
      \vspace{0.3em}

      {\small
      \includegraphics[width=\linewidth]{tab_segmentation_iou.pdf}}
    \end{column}
  \end{columns}
\end{frame}

% ── 9.2 Stellar Parameter Estimation ─────────────────────────────────────────
\begin{frame}{Stellar parameter estimation}
  {\small Gaia + DESI stellar validation set (never seen during pre-training).
    Frozen AION-1-B encoder $+$ cross-attention pooling $+$ linear probe. $R^2$.}
  \vspace{0.3em}
  \centering
  \includegraphics[width=0.98\textwidth]{tab_stellar_properties_full.pdf}
\end{frame}

% ── 9.2 Stellar Parameter Comparison ─────────────────────────────────────────
\begin{frame}{Stellar parameters: comparison with specialist model}
  {\small Transfer to APOGEE stellar labels from Gaia XP coefficients only
    (first 32 BP + 32 RP coefficients).
    AION-1-B is \emph{never} pre-trained on APOGEE data; the baseline
    \parencite{leung2023stellar} is explicitly pre-trained on APOGEE.
    Metric: standard deviation of residuals ($\downarrow$).}
  \vspace{0.5em}
  \centering
  \includegraphics[width=0.75\textwidth]{tab_stellar_properties_comparison.pdf}
\end{frame}

% ── 9.2 Low-Data Regime ───────────────────────────────────────────────────────
\begin{frame}{Low-data regime}
  \begin{columns}[T]
    \begin{column}{0.32\textwidth}
      \centering
      \includegraphics[width=\linewidth]{galaxy_scaling.png}
      \par\smallskip
      {\footnotesize Galaxy properties ($R^2$)}
    \end{column}
    \begin{column}{0.32\textwidth}
      \centering
      \includegraphics[width=\linewidth]{gz10.png}
      \par\smallskip
      {\footnotesize Morphology (accuracy)}
    \end{column}
    \begin{column}{0.32\textwidth}
      \centering
      \includegraphics[width=\linewidth]{desiddpayne_feh_scaling.pdf}
      \par\smallskip
      {\footnotesize Stellar properties ($R^2$)}
    \end{column}
  \end{columns}
  \vspace{0.4em}
  {\small
    With $10^2\!-\!10^3$ labels: AION-1 reaches $R^2\!\sim\!0.5$ and
    ${\geq}80\%$ morphology accuracy; supervised baselines remain near
    zero or below 70\%.\\[0.3em]
    AION-1 also saturates at ${\sim}10^3\!-\!10^4$ examples — an order of
    magnitude faster than supervised models.
  }
\end{frame}

% ── 9.3 Retrieval Datasets ────────────────────────────────────────────────────
\begin{frame}{Retrieval datasets}
  {\small High-confidence query sets for the rare object retrieval experiments.
    Citizen-science morphologies from Galaxy Zoo–DECaLS
    \parencite{walmsley2022gzdecals}; strong lenses from HSC lens-finding
    catalogs cross-matched with Legacy Survey.}
  \vspace{0.5em}
  \centering
  \includegraphics[width=\textwidth]{tab_retrieval_query_stats.pdf}
\end{frame}

% ── 9.3 Rare Object Detection ─────────────────────────────────────────────────
\begin{frame}{Rare object detection}
  \begin{columns}[T]
    \begin{column}{0.44\textwidth}
      \centering
      \includegraphics[width=\linewidth]{retrieval_figure.png}
    \end{column}
    \begin{column}{0.53\textwidth}
      \textbf{Cosine-similarity retrieval} in embedding space.
      Zero-shot, no task-specific labels.
      \vspace{0.3em}

      \begin{align*}
        S_c(\mathbf{x}_q,\mathbf{x}_c) &=
          \frac{\mathbf{x}_q^{\mathsf{T}}\mathbf{x}_c}
               {\|\mathbf{x}_q\|_2\,\|\mathbf{x}_c\|_2} \\[3pt]
        \mathrm{DCG@}k &= \sum_{i=1}^{k}\tfrac{2^{r_i}-1}{\log_2(i+1)} \\[3pt]
        \mathrm{nDCG@}k &= \tfrac{\mathrm{DCG@}k}{\mathrm{IDCG@}k}
      \end{align*}
      \vspace{0.1em}

      {\small
      \includegraphics[width=\linewidth]{tab_retrieval-scores.pdf}}
    \end{column}
  \end{columns}
\end{frame}

% ── 9.4 Emergent Transfer: HSC → DESI ────────────────────────────────────────
\begin{frame}{Cross-modal generation (in unseen combinations!)}
  \centering
  \includegraphics[width=0.88\textwidth]{hsc_from_desi.png}
  \par\smallskip
  {\small AION-1 samples DESI spectra (blue) conditioned on HSC images (insets),
    matching ground truth (black).\\
    HSC–DESI pairs were \emph{never seen during pre-training}: the model
    infers the mapping transitively via shared intermediate surveys.}
\end{frame}

% ── 9.4 Cross-Survey Morphology Transfer ─────────────────────────────────────
\begin{frame}{Cross-survey morphology transfer}
  \begin{columns}[T]
    \begin{column}{0.48\textwidth}
      {\small Train a GZ10 classifier on \textbf{Legacy Survey} embeddings.
      Apply the \emph{same frozen head} — without fine-tuning — to
      \textbf{HSC} embeddings.\\[0.5em]
      Surveys differ in depth, filter set ($\{g,r,i,z,y\}$ vs.\
      $\{g,r,i,z\}$), and pixel scale.}
    \end{column}
    \begin{column}{0.48\textwidth}
      {\small
      \includegraphics[width=\linewidth]{tab_transfer_ls_hsc.pdf}}
      \vspace{0.5em}

      {\small Zero-shot GZ10 accuracy (\%). LS = Legacy Survey (train),
        HSC = eval. AION-1 embeddings are largely survey-invariant.}
    \end{column}
  \end{columns}
\end{frame}

% ── §10 Future Work ───────────────────────────────────────────────────────────
\begin{frame}[standout]
  Conclusions
\end{frame}

\begin{frame}{Future work}
  \textbf{Architecture}
  \begin{itemize}
    \item Replace quantised tokens with \textbf{continuous tokens} to lift
          the information ceiling imposed by codebook discretisation
    \item Better decoder: current masked-token decoder does not produce a
          proper conditional distribution given the encoder embedding —
          candidates include autoregressive modelling, diffusion-based
          decoding, or dropping decoding entirely in favour of a
          \textbf{JEPA-style} objective
    \item Better pooling: mean pooling is suboptimal; consider
          self-supervised discriminative post-training for
          task-aware summary embeddings
  \end{itemize}
  \vspace{0.5em}
  \textbf{Community adoption}
  \begin{itemize}
    \item Integration into survey pipelines as a source of ML-inferred
          data products
    \item Best use-cases today: \textbf{embeddings} for parameter estimation
          and \textbf{similarity retrieval} — generation not yet recommended
          for production use
  \end{itemize}
\end{frame}

% ── §11 Conclusions ───────────────────────────────────────────────────────────
\begin{frame}{Conclusions}
  \begin{itemize}
    \item \textbf{Integrates} imaging, spectroscopy, and scalar data across
          five large-scale surveys via modality-specific tokenisation and a
          shared masked-modelling objective
    \item \textbf{Achieves or surpasses state-of-the-art} on galaxy and
          stellar parameter estimation, morphology classification, image
          segmentation, and spectral super-resolution
    \item \textbf{Excels in the low-data regime} — competitive performance
          with $10^2$–$10^3$ labels, two to three orders of magnitude fewer
          than fully supervised baselines
    \item \textbf{Enables zero-shot semantic retrieval} of rare objects
          (mergers, strong gravitational lenses) without task-specific labels
    \item \textbf{Enables cross-survey transfer}: representations are largely
          agnostic to instrument-specific characteristics
    \item A scalable, open-source blueprint for multimodal foundation models
          in other scientific domains
  \end{itemize}
\end{frame}

% ── §12 Resources ─────────────────────────────────────────────────────────────
\begin{frame}{Resources}
  \textbf{Multimodal Universe}\\[0.2em]
  {\small\begin{tabular}{@{}ll@{}}
    Paper   & \href{https://arxiv.org/abs/2412.02527}{arXiv:2412.02527} \\
    Repo    & \href{https://github.com/MultimodalUniverse/MultimodalUniverse}%
              {\texttt{github.com/MultimodalUniverse}} \\
    Data    & \href{https://huggingface.co/MultimodalUniverse}%
              {\texttt{huggingface.co/MultimodalUniverse}} \\
            & \href{https://app.globus.org/file-manager?origin_id=57136152-fc1d-418e-b74e-75ca52bddd21}%
              {Globus} (recommended for large downloads) \\
  \end{tabular}}

  \vspace{0.8em}
  \textbf{AION-1}\\[0.2em]
  {\small\begin{tabular}{@{}ll@{}}
    Paper   & \href{https://arxiv.org/abs/2510.17960}{arXiv:2510.17960}
              (NeurIPS 2025) \\
    Repo    & \href{https://github.com/PolymathicAI/AION}%
              {\texttt{github.com/PolymathicAI/AION}} \\
    Weights & \href{https://huggingface.co/PolymathicAI}%
              {\texttt{huggingface.co/PolymathicAI}} \\
  \end{tabular}}
\end{frame}

% ══════════════════════════════════════════════════════════════════════════════
\appendix

\begin{frame}[standout]
  Appendix
\end{frame}

% ── Appendix: Latent Diffusion Models ────────────────────────────────────────
\begin{frame}{Semantic versus perceptual compression}
  \centering
  \includegraphics[width=\textwidth, height=0.78\textheight, keepaspectratio]{generativevscompressive4.jpg}
  \par\smallskip
  {\small Illustrating perceptual and semantic compression: a generative model
    must only model a low-dimensional latent space, leaving perceptual detail
    to the decoder. \parencite{rombach2021ldm}}
\end{frame}

% ── Appendix: VQ-VAE ──────────────────────────────────────────────────────────
\begin{frame}{VQ-VAE}
  \centering
  \includegraphics[width=\textwidth, height=0.78\textheight, keepaspectratio]{Figure1_9.png}
  \par\smallskip
  {\small Left: VQ-VAE architecture. Right: embedding space — encoder output
    $z(x)$ is mapped to the nearest codebook vector $e_2$; the gradient
    $\nabla_z\mathcal{L}$ (red) pushes the encoder towards that entry.
    \parencite{oord2017vqvae}}
\end{frame}

% ── Appendix: MaskGIT iterative decoding ─────────────────────────────────────
\begin{frame}{MaskGIT}
  \centering
  \includegraphics[width=\textwidth, height=0.78\textheight, keepaspectratio]{iterative_decoding.pdf}
  \par\smallskip
  {\small Rows 1\,\&\,3: latent masks; rows 2\,\&\,4: generated samples.
    MaskGIT fills tokens in parallel under a cosine mask schedule,
    completing in 8 iterations vs.\ 256 for autoregressive decoding.
    \parencite{chang2022maskgit}}
\end{frame}

\end{document}
