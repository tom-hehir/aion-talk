# macOS metadata
.DS_Store

# Python virtual environment
.venv

# Transient build artefacts
presentation/build

# Downloaded paper resources (populate with: make fetch)
resources

# Presentation PDF (use git add -f + tag for milestones)
/presentation.pdf
